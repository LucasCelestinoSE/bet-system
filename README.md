# bet-system
## Para instalar

# Para rodar
sudo docker-compose up --build
sudo docker compose stop ( para os cointaiers para desligar o PC )
sudo docker compose start (Para ligar tudo de novo em segundos) 
